The modification should allow the package to be built
with on new platforms: hurd and freebsd
Index: kicad-4.0.2+dfsg1/include/kiway.h
===================================================================
--- kicad-4.0.2+dfsg1.orig/include/kiway.h
+++ kicad-4.0.2+dfsg1/include/kiway.h
@@ -120,6 +120,8 @@ as such!  As such, it is OK to use UTF8
  #define LIB_ENV_VAR    wxT( "DYLD_LIBRARY_PATH" )
 #elif defined(__MINGW32__)
  #define LIB_ENV_VAR    wxT( "PATH" )
+# else
+ #define LIB_ENV_VAR    wxT( "LD_LIBRARY_PATH" )
 #endif
 
 
